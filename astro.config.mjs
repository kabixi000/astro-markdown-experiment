import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // github pages 用設定
  site: 'https://kabixi000.github.io',
  base: '/astro-markdown-experiment'
})
